<%= turbo_stream.replace(@square) do %>
  <%= turbo_frame_tag(@square) do %>
    <%= render(SquareComponent.new(@square)) %>
  <% end %>
<% end %>

<%= turbo_stream.update(current_user) do %>
  <%= current_user.name %> $<%= current_user.squares.length * @square.game.square_price %>
<% end %>
